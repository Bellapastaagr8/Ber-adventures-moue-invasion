function enums(){
	enum AI{
		dumb,
		agressive,
		flyag,
		flying,
		still,
		bomb,
		intimidatingIntimidated,
		ghost,
	}
	enum States{
		play,
		bribery,
		next_room,
		shop,
		title,
		dead,
		deadScreen,
		youWin,
		pause,
	}
}